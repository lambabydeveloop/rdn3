<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>С Днём влюбленных, Дорогая София 💕 Любовь всей моей жизни</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Montserrat:wght@300;400;600&family=Caveat:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; user-select: none; -webkit-user-select: none; }
        body {
            min-height: 100vh;
            background: linear-gradient(145deg, #2b1a2e 0%, #4a2c3b 70%, #6d3f4f 100%);
            font-family: 'Montserrat', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
        }
        .bg-hearts { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 0; }
        .heart-float {
            position: absolute; font-size: 1.5rem; opacity: 0.4; color: rgba(255, 200, 220, 0.7);
            animation: floatUp 12s linear infinite; user-select: none;
        }
        @keyframes floatUp {
            0% { transform: translateY(100vh) rotate(0deg) scale(0.8); opacity: 0.1; }
            20% { opacity: 0.6; }
            100% { transform: translateY(-20vh) rotate(20deg) scale(1.5); opacity: 0; }
        }
        .valentine-card {
            position: relative; z-index: 10; max-width: 1100px; width: 100%;
            background: rgba(255, 240, 245, 0.15); backdrop-filter: blur(12px);
            border-radius: 60px 20px 60px 20px;
            box-shadow: 0 30px 50px rgba(0,0,0,0.5), 0 0 0 2px rgba(255,215,220,0.3) inset, 0 0 30px #ffa5b9;
            padding: 35px 30px; border: 1px solid rgba(255,160,180,0.6);
            animation: cardGlow 3s infinite alternate;
        }
        @keyframes cardGlow {
            0% { box-shadow: 0 30px 50px rgba(0,0,0,0.5), 0 0 0 2px rgba(255,215,220,0.3) inset, 0 0 30px #ffa5b9; }
            100% { box-shadow: 0 30px 60px rgba(255,105,180,0.4), 0 0 0 4px rgba(255,220,240,0.7) inset, 0 0 50px #ffb6c1; }
        }
        .content-row { display: flex; flex-wrap: wrap; gap: 25px; align-items: center; justify-content: center; }
        .text-section { flex: 1 1 280px; text-align: center; padding: 15px; animation: softAppear 1.2s ease-out; }
        @keyframes softAppear { 0% { opacity:0; transform:translateY(20px); } 100% { opacity:1; transform:translateY(0); } }
        .main-title {
            font-family: 'Great Vibes', cursive; font-size: clamp(3rem, 12vw, 5rem); font-weight: 400;
            color: #fff9f9; text-shadow: 0 0 15px #ff7e9f, 2px 4px 10px #ab4e6b; margin-bottom:5px;
            animation: titleTwinkle 3s infinite;
        }
        @keyframes titleTwinkle {
            0% { text-shadow:0 0 15px #ff7e9f, 2px 4px 10px #ab4e6b; }
            50% { text-shadow:0 0 30px #ffb6c1, 0 0 20px #ff3b6f, 4px 6px 12px #6b2e42; }
            100% { text-shadow:0 0 15px #ff7e9f, 2px 4px 10px #ab4e6b; }
        }
        .lovely-message {
            font-size: clamp(1.1rem,4vw,1.6rem); color:#ffecf0; line-height:1.6; margin:20px 0 15px;
            background: rgba(80,30,40,0.25); padding:20px 15px; border-radius:50px 20px 50px 20px;
            backdrop-filter: blur(5px); border-left:6px solid #ffa0b5; border-right:6px solid #ffa0b5;
            box-shadow:0 5px 20px rgba(0,0,0,0.2); transition:0.2s;
        }
        .lovely-message:hover { transform: scale(1.02) rotate(0.5deg); background: rgba(100,40,55,0.35); border-color:#ffc0cb; }
        .video-section { flex: 1 1 380px; min-width:300px; animation: softAppear 1.2s 0.2s both; }
        .video-frame {
            background: linear-gradient(45deg, #ffb7c5, #ff8aa1, #ffb7c5); background-size:200% 200%; padding:12px;
            border-radius:45px 15px 45px 15px; box-shadow:0 20px 30px rgba(0,0,0,0.6), 0 0 30px #fda5b4;
            animation: borderGradient 5s ease infinite, framePulse 2s infinite alternate;
        }
        @keyframes borderGradient { 0% { background-position:0% 50%; } 50% { background-position:100% 50%; } 100% { background-position:0% 50%; } }
        @keyframes framePulse { 0% { box-shadow:0 20px 30px rgba(0,0,0,0.6), 0 0 30px #fda5b4; } 100% { box-shadow:0 25px 40px #b34e6b, 0 0 50px #ffd0d9; } }
        .video-wrapper { position:relative; width:100%; padding-bottom:56.25%; border-radius:35px 10px 35px 10px; overflow:hidden; background:#1e0f13; }
        .video-wrapper iframe { position:absolute; top:0; left:0; width:100%; height:100%; border:none; border-radius:35px 10px 35px 10px; }

        /* интерактив */
        .interactive-zone {
            margin-top:40px; display:flex; flex-wrap:wrap; align-items:center; justify-content:space-around; gap:20px;
            background:rgba(255,180,200,0.18); border-radius:100px 20px 100px 20px; padding:20px 25px; backdrop-filter:blur(5px);
            border:1px dashed #ffb8c9;
        }
        .heart-click-area { display:flex; flex-direction:column; align-items:center; gap:10px; cursor:pointer; transition:0.3s; }
        .big-heart {
            font-size:4.5rem; filter:drop-shadow(0 0 15px #ff3b6f); animation: heartBeat 1.1s infinite ease-in-out; transition:0.2s;
            -webkit-user-select: none; -moz-user-select: none; -ms-user-select: none; user-select: none;
        }
        .big-heart:active { transform:scale(1.5); }
        @keyframes heartBeat { 0%{transform:scale(1);} 25%{transform:scale(1.2);} 40%{transform:scale(1);} 60%{transform:scale(1.15);} 100%{transform:scale(1);} }
        .click-counter {
            font-size:1.8rem; font-weight:600; color:#ffd9e2; text-shadow:0 0 10px #c13b5c;
            background:rgba(60,20,30,0.5); padding:8px 25px; border-radius:40px; backdrop-filter:blur(4px); border:2px solid #ffa5b9;
            user-select: none;
        }
        .hearts-burst { font-size:2rem; letter-spacing:10px; animation: burstGlow 1.2s infinite; user-select: none; }
        @keyframes burstGlow { 0%{opacity:0.4; text-shadow:0 0 5px white;} 50%{opacity:1; text-shadow:0 0 25px #ffc0cb,0 0 40px #ff6f91;} 100%{opacity:0.4; text-shadow:0 0 5px white;} }
        .gift-button {
            background:linear-gradient(145deg, #ff98af, #ff6b8f); border:none; color:#2d131c; font-size:1.5rem; font-weight:bold;
            padding:16px 32px; border-radius:60px 15px 60px 15px; cursor:pointer; box-shadow:0 10px 0 #8f3f54, 0 15px 30px #330e18;
            transition:0.07s linear; text-transform:uppercase; letter-spacing:2px; border:2px solid #fff2f5;
            animation: buttonSoft 2s infinite;
        }
        .gift-button:active { transform:translateY(8px); box-shadow:0 5px 0 #8f3f54, 0 20px 30px #1f070e; }
        @keyframes buttonSoft { 0%{box-shadow:0 10px 0 #8f3f54,0 15px 30px #330e18;} 50%{box-shadow:0 10px 0 #b1546d,0 20px 40px #5e1f2f;} 100%{box-shadow:0 10px 0 #8f3f54,0 15px 30px #330e18;} }

        /* Новая кнопка для открытки */
        .postcard-button {
            background:linear-gradient(145deg, #d48c9c, #b95e7a); border:none; color:#fff0f3; font-size:1.3rem; font-weight:bold;
            padding:14px 28px; border-radius:50px 10px 50px 10px; cursor:pointer; box-shadow:0 8px 0 #632f41, 0 12px 25px #2b101a;
            transition:0.07s linear; text-transform:none; letter-spacing:1px; border:2px solid #ffd0da; font-family:'Great Vibes', cursive;
            animation: postcardSoft 2.5s infinite; margin-top:15px;
        }
        .postcard-button:active { transform:translateY(6px); box-shadow:0 4px 0 #632f41, 0 15px 25px #2b101a; }
        @keyframes postcardSoft { 0%{box-shadow:0 8px 0 #632f41, 0 12px 25px #2b101a;} 50%{box-shadow:0 8px 0 #81445b, 0 18px 30px #4a1e2c;} 100%{box-shadow:0 8px 0 #632f41, 0 12px 25px #2b101a;} }

        .tiny-hearts { position:relative; height:50px; }
        .tiny-hearts span { position:absolute; font-size:1.8rem; animation: scatter 3s infinite alternate; }
        @keyframes scatter { 0%{transform:translate(0,0) scale(0.8); opacity:0.3;} 100%{transform:translate(20px,-20px) scale(1.3); opacity:1;} }
        .footer-note { margin-top:15px; text-align:center; color:#ffe3ea; font-size:1.1rem; opacity:0.9; }
        .invitation { font-size:1.3rem; font-family:'Great Vibes', cursive; color:#fff2f5; text-align:center; }
        .invitation span { display:inline-block; animation: wave 2s infinite; }
        @keyframes wave { 0%,100%{transform:rotate(0deg);} 25%{transform:rotate(10deg) scale(1.1);} 75%{transform:rotate(-5deg) scale(1.05);} }

        /* модальное окно (за десятки) - первое, с фразой */
        .modal-overlay {
            position: fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(30,10,20,0.7);
            backdrop-filter:blur(5px); z-index:1000; display:flex; justify-content:center; align-items:center;
            opacity:0; visibility:hidden; transition: opacity 0.4s ease, visibility 0.4s;
        }
        .modal-overlay.show { opacity:1; visibility:visible; }
        .modal-content {
            background: linear-gradient(145deg, #ffdae3, #ffb3c6); max-width:400px; width:90%;
            padding:30px 25px; border-radius:80px 20px 80px 20px; box-shadow:0 30px 50px #400d1e, 0 0 0 6px #fff3f5 inset;
            text-align:center; transform: scale(0.7) rotate(-5deg); transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
            border:2px solid white;
        }
        .modal-overlay.show .modal-content { transform: scale(1) rotate(0deg); }
        .modal-content p {
            font-family: 'Great Vibes', cursive; font-size: 2.3rem; color:#511f30; margin-bottom:15px;
            text-shadow: 2px 2px 0 #ffb8cb;
        }
        .modal-content .small-heart { font-size:3rem; animation: modalHeart 0.9s infinite; }
        @keyframes modalHeart { 0%{transform:scale(1);} 50%{transform:scale(1.3); color:#ff3366;} }
        .modal-close {
            background: #a1415b; border:none; color:white; font-size:1.5rem; padding:10px 35px;
            border-radius:40px 10px 40px 10px; margin-top:20px; cursor:pointer; border:2px solid #ffd0d9;
            transition:0.2s; font-weight:bold;
        }
        .modal-close:hover { background:#c95b79; transform:scale(1.05); }

        /* попап с цитатой из фильма (с крестиком и автозакрытием) */
        .movie-popup {
            position: fixed; bottom:30px; right:30px; background: #fff0f5; color:#2f1422;
            padding:0; border-radius:70px 20px 70px 20px; box-shadow: 0 20px 50px #2c0b1c;
            z-index: 2000; max-width:400px; width:calc(100% - 40px); border:6px solid #ff99bb;
            font-family:'Montserrat', sans-serif; opacity:0; transform: translateX(60px) scale(0.5);
            transition: 0.4s; pointer-events:none; overflow:hidden;
        }
        .movie-popup.show { opacity:1; transform: translateX(0) scale(1); pointer-events:auto; }
        .movie-popup-header {
            background: #ffb6c9; padding:12px 20px; display:flex; justify-content:space-between; align-items:center;
            border-bottom:2px solid white; font-family:'Great Vibes', cursive; font-size:1.8rem; color:#4d1f2f;
        }
        .movie-popup-header span { cursor:pointer; font-size:2.5rem; line-height:1; padding:0 10px; }
        .movie-popup-header span:hover { color:#910033; transform:scale(1.2); }
        .movie-quote {
            padding:25px 20px 15px; font-size:1.7rem; font-family:'Great Vibes', cursive; text-align:center;
            background: #ffeef2; color:#2c0b1a;
        }
        .movie-from {
            text-align:right; padding:0 20px 20px; font-style:italic; color:#a1415b; font-size:1.2rem;
        }

        /* НОВЫЙ ПРОМЕЖУТОЧНЫЙ ЭКРАН - ПЕЧАТЬ (появляется после нажатия на кнопку) */
        .seal-modal {
            position: fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(60,20,35,0.9);
            backdrop-filter:blur(8px); z-index:4000; display:flex; justify-content:center; align-items:center;
            opacity:0; visibility:hidden; transition: opacity 0.4s ease, visibility 0.4s;
        }
        .seal-modal.show { opacity:1; visibility:visible; }
        .seal-content {
            background: #fcedf0; max-width:450px; width:90%; padding:30px 20px; border-radius:200px 30px 200px 30px;
            box-shadow: 0 30px 60px #2b0c18, 0 0 0 15px #ffb6c9 inset; text-align:center; border:6px solid white;
            transform: scale(0.5); transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        }
        .seal-modal.show .seal-content { transform: scale(1); }
        .seal-wax {
            background: #b53b5a; width:180px; height:180px; border-radius:50%; margin:10px auto 25px;
            display:flex; align-items:center; justify-content:center; font-size:5rem; border:10px solid #ffdbe7;
            box-shadow: 0 15px 0 #772b41, 0 20px 30px #3e1322; cursor:pointer; transition: 0.1s;
            animation: sealPulse 1.5s infinite;
        }
        @keyframes sealPulse {
            0% { box-shadow: 0 15px 0 #772b41, 0 20px 30px #3e1322; }
            50% { box-shadow: 0 18px 0 #8f3550, 0 25px 40px #571e30; }
            100% { box-shadow: 0 15px 0 #772b41, 0 20px 30px #3e1322; }
        }
        .seal-wax:active { transform: scale(0.95); }
        .seal-crack {
            font-size: 2.2rem; color:#511e2e; margin:20px 0; font-family: 'Great Vibes', cursive;
        }
        .seal-counter {
            background: #ffa5b9; padding:15px 25px; border-radius:50px; display:inline-block; font-size:2.5rem;
            font-weight:bold; color:#2e0e18; border:4px solid white; margin-bottom:20px;
        }

        /* МОДАЛЬНОЕ ОКНО-ОТКРЫТКА (снимаем печать) - прокрутка без скроллбара */
        .postcard-modal {
            position: fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(50,15,25,0.8);
            backdrop-filter:blur(6px); z-index:4500; display:flex; justify-content:center; align-items:flex-start;
            opacity:0; visibility:hidden; transition: opacity 0.5s ease, visibility 0.5s;
            overflow-y: auto;
            padding: 20px 0;
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
        .postcard-modal::-webkit-scrollbar { display: none; }
        .postcard-modal.show { opacity:1; visibility:visible; }
        .postcard-inner {
            background: #fcede6; max-width:600px; width:90%; padding:35px 25px 30px;
            border-radius:120px 25px 120px 25px; box-shadow: 0 40px 70px #1f0a12, 0 0 0 12px #ffc2cf inset, 0 0 0 15px #f99bb6;
            text-align:center; position:relative; transform: scale(0.3) rotate(-15deg); transition: transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            border: 4px solid #ffdbea;
            margin: auto;
        }
        .postcard-modal.show .postcard-inner { transform: scale(1) rotate(0deg); }
        .postcard-stamp {
            position:absolute; top:-15px; right:20px; background:#ffd966; padding:10px 20px; border-radius:50% 50% 0 50%;
            font-size:2rem; transform:rotate(15deg); border:4px dashed white; color:#a13555; box-shadow:0 5px 0 #87455b;
        }
        .postcard-wax-seal {
            position:absolute; bottom:-15px; left:20px; background:#b3415d; width:70px; height:70px;
            border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:2.5rem;
            border:5px solid #ffd6e0; color:#ffdcaa; box-shadow:0 8px 0 #64273b; transform:rotate(-10deg);
            animation: sealGlow 2s infinite;
        }
        @keyframes sealGlow { 0%{box-shadow:0 8px 0 #64273b, 0 0 10px #ff99bb;} 50%{box-shadow:0 8px 0 #64273b, 0 0 30px #ffb3c6;} }

        /* Контейнер для печатающегося текста - тоже с прокруткой и без скроллбара */
        .postcard-message {
            font-family: 'Caveat', cursive; font-size: 2.2rem; color:#511a2e; margin:35px 0 20px;
            background: rgba(255,220,230,0.5); padding:25px; border-radius:80px 15px 80px 15px;
            border: 2px dashed #ff93af; line-height:1.5;
            max-height: 250px;
            overflow-y: auto;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            text-shadow: 1px 1px 0 #ffeef2;
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
        .postcard-message::-webkit-scrollbar { display: none; }
        .postcard-message span {
            display: inline-block;
            white-space: pre-wrap;
            word-break: break-word;
        }

        .cursor {
            display: inline-block;
            width: 4px;
            height: 2.2rem;
            background-color: #a13555;
            margin-left: 5px;
            animation: blink 0.8s infinite;
            vertical-align: middle;
        }
        @keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0;} }

        .postcard-footer {
            font-family: 'Great Vibes', cursive; font-size: 1.8rem; color:#83264b; margin-top:15px; letter-spacing:2px;
        }
        .postcard-close-btn {
            background: #a53f5a; border:none; color:white; font-size:1.5rem; padding:12px 35px;
            border-radius:40px 10px 40px 10px; margin-top:25px; cursor:pointer; border:3px solid #ffc0d0;
            font-weight:bold; transition:0.2s;
        }
        .postcard-close-btn:hover { background:#c75e7a; transform:scale(1.05); }

        /* НОВОЕ МОДАЛЬНОЕ ОКНО ДЛЯ ВИДЕО-ПРИЗНАНИЯ (тоже с прокруткой без скроллбара) */
        .video-confession-modal {
            position: fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(40,10,25,0.85);
            backdrop-filter:blur(8px); z-index:3500; display:flex; justify-content:center; align-items:flex-start;
            opacity:0; visibility:hidden; transition: opacity 0.5s ease, visibility 0.5s;
            overflow-y: auto; padding:20px 0;
            -ms-overflow-style: none; scrollbar-width: none;
        }
        .video-confession-modal::-webkit-scrollbar { display: none; }
        .video-confession-modal.show { opacity:1; visibility:visible; }
        .video-confession-content {
            background: linear-gradient(145deg, #ffe3ec, #ffb6cc); max-width:650px; width:95%;
            padding:30px 20px; border-radius:100px 20px 100px 20px; box-shadow:0 30px 60px #2d0d1b, 0 0 0 10px #fff0f5 inset;
            text-align:center; transform: scale(0.3) rotate(-5deg); transition: transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            border: 4px solid white; margin: auto;
        }
        .video-confession-modal.show .video-confession-content { transform: scale(1) rotate(0deg); }
        .video-confession-title {
            font-family: 'Great Vibes', cursive; font-size: 2.8rem; color:#501f32; margin-bottom:15px;
            text-shadow: 2px 2px 0 #ffb0c3;
        }
        .video-confession-frame {
            border-radius: 60px 10px 60px 10px; overflow: hidden; border: 6px solid #ff99bb;
            box-shadow: 0 15px 30px #411126; margin-bottom: 15px;
        }
        .video-confession-frame iframe { width: 100%; height: 280px; display: block; }
        .toggle-transcript-btn {
            background: #a1415b; border: none; color: white; font-size: 1.4rem; padding: 12px 30px;
            border-radius: 50px 10px 50px 10px; cursor: pointer; margin: 15px 0; border: 3px solid #ffd0da;
            font-weight: bold; transition: 0.2s; font-family: 'Montserrat', sans-serif;
        }
        .toggle-transcript-btn:hover { background: #c95b79; transform: scale(1.02); }
        .transcript-text {
            font-family: 'Caveat', cursive; font-size: 1.7rem; color: #2e1020; background: #fff2f7;
            padding: 20px; border-radius: 40px 10px 40px 10px; border: 2px solid #ff99bb;
            margin: 10px 0 15px; max-height: 0; overflow: hidden; transition: max-height 0.6s ease, padding 0.6s ease;
            text-align: left; white-space: pre-line;
            -ms-overflow-style: none; scrollbar-width: none;
        }
        .transcript-text::-webkit-scrollbar { display: none; }
        .transcript-text.show { max-height: 600px; padding: 20px; overflow-y: auto; }
        .close-confession-btn {
            background: #7b364b; border: none; color: white; font-size: 1.4rem; padding: 10px 35px;
            border-radius: 40px 10px 40px 10px; cursor: pointer; border: 3px solid #ffc0d0; margin-top: 10px;
        }
        .close-confession-btn:hover { background: #9e4b63; }

        @media (max-width:700px) {
            .valentine-card { padding:20px 15px; }
            .interactive-zone { flex-direction:column; border-radius:50px 20px 50px 20px; }
            .big-heart { font-size:3.5rem; }
            .movie-popup { left:20px; right:20px; max-width:none; }
            .postcard-message { font-size:1.8rem; max-height:200px; }
            .video-confession-frame iframe { height:200px; }
        }
    </style>
</head>
<body>
<div class="bg-hearts" id="bgHearts"></div>

<!-- Модальное окно для кратных 10 кликов (первое, с фразой) -->
<div class="modal-overlay" id="decadeModal">
    <div class="modal-content">
        <div class="small-heart">💖</div>
        <p id="modalPhrase">Ты у меня в сердечке 💓</p>
        <div class="small-heart">💖</div>
        <button class="modal-close" id="closeModal">Закрыть</button>
    </div>
</div>

<!-- НОВОЕ МОДАЛЬНОЕ ОКНО: видео-признание (появляется только один раз после первых 10 кликов) -->
<div class="video-confession-modal" id="videoConfessionModal">
    <div class="video-confession-content">
        <div class="video-confession-title">💕 Видео-признание для тебя 💕</div>
        <div class="video-confession-frame">
            <iframe src="https://www.youtube.com/embed/5CGdnIkuRMI?autoplay=0&mute=1&playsinline=1"
                    title="YouTube video confession" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
        </div>
        <button class="toggle-transcript-btn" id="toggleTranscriptBtn">📜 показать текстовую расшифровку</button>
        <div class="transcript-text" id="transcriptText">
            Только мне одному, быть может, известно, что ты лучшая женщина на свете.
            Похоже, только я один понимаю, как замечательно всё, что ты делаешь.
            Как восхитительно ты всё воспринимаешь, как разговариваешь с людьми и как почти во всём проявляются твои лучшие качества: твои доброта и честность.
            И мне кажется, этого многие не замечают. И я очень рад и горжусь, что первым заметил это.
        </div>
        <button class="close-confession-btn" id="closeConfessionBtn">Закрыть</button>
    </div>
</div>

<!-- Попап с цитатой из фильма (с крестиком, авто-исчезновение) -->
<div class="movie-popup" id="moviePopup">
    <div class="movie-popup-header">
        🎬 Кино-признание
        <span id="closeMoviePopup" style="cursor:pointer;">✕</span>
    </div>
    <div class="movie-quote" id="movieQuote">“Ты заставляешь меня хотеть быть лучше.”</div>
    <div class="movie-from" id="movieFrom">— Из фильма</div>
</div>

<!-- ПРОМЕЖУТОЧНЫЙ ЭКРАН - ПЕЧАТЬ (появляется после нажатия на кнопку открытки) -->
<div class="seal-modal" id="sealModal">
    <div class="seal-content">
        <div class="seal-crack">💌 сломай печать 💌</div>
        <div class="seal-wax" id="sealWax">❤️</div>
        <div class="seal-counter" id="sealCounter">0 / 7</div>
        <div style="color:#7a3850; font-size:1.3rem;">нажимай на сердечко</div>
    </div>
</div>

<!-- МОДАЛЬНОЕ ОКНО-ОТКРЫТКА "Снять печать" с прокруткой без скроллбара -->
<div class="postcard-modal" id="postcardModal">
    <div class="postcard-inner">
        <div class="postcard-stamp">💌</div>
        <div class="postcard-wax-seal">❤️</div>
        <div class="postcard-message" id="postcardMessage">
            <span id="typedText"></span><span class="cursor" id="cursor"></span>
        </div>
        <div class="postcard-footer">С любовью навеки 💕</div>
        <button class="postcard-close-btn" id="closePostcard">Закрыть открытку</button>
    </div>
</div>

<main class="valentine-card">
    <div class="content-row">
        <div class="text-section">
            <h1 class="main-title">С днём влюбленных, Милая моя!</h1>
            <div class="lovely-message" id="mainMessage">
                ❤️ Пусть этот день наполнит сердце <br>
                нежностью, радостью и теплом. <br>
                Ты — самое прекрасное, что есть в этой вселенной. <br>
                <span style="font-size: 1.8rem; display: inline-block; animation: wave 2s infinite;">✨</span>
            </div>
            <div class="tiny-hearts"><span style="left:-10px;">💖</span><span style="left:40px; animation-delay:0.5s;">💘</span><span style="left:90px; animation-delay:1s;">💝</span></div>
        </div>
        <div class="video-section">
            <div class="video-frame">
                <div class="video-wrapper">
                    <iframe src="https://www.youtube.com/embed/u56tXhIinlk?autoplay=0&mute=1&playsinline=1&loop=1&playlist=u56tXhIinlk"
                            title="YouTube" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                </div>
            </div>
            <p style="color:#ffd2dc; margin-top:10px; text-align:center; font-style:italic;">💞 мелодия любви</p>
        </div>
    </div>

    <div class="interactive-zone">
        <div class="heart-click-area" id="heartClickArea">
            <div class="big-heart" id="bigHeart">❤️</div>
            <div class="click-counter" id="clickCounter">0</div>
            <span style="color:#ffcdd6;">жми на сердце</span>
        </div>
        <div class="hearts-burst" id="burstHearts">💖💗💘💝</div>
        <button class="gift-button" id="surpriseBtn">Сюрприз</button>
    </div>

    <!-- Новая кнопка "Снять печать с открытки" -->
    <div style="text-align: center; margin-top: 20px;">
        <button class="postcard-button" id="postcardBtn">💮 Снять печать с открытки 💮</button>
    </div>

    <div class="footer-note">
        <span class="invitation"><span>🥰</span> Ты — моя валентинка навеки <span>🥰</span></span>
    </div>
</main>

<script>
    (function() {
        // фоновые сердечки
        const bgContainer = document.getElementById('bgHearts');
        for (let i = 0; i < 18; i++) {
            const heart = document.createElement('div');
            heart.className = 'heart-float';
            const symbols = ['❤️','🧡','💛','💚','💙','💜','🤍','💖','💗','💓','💕'];
            heart.textContent = symbols[Math.floor(Math.random() * symbols.length)];
            heart.style.left = Math.random() * 100 + '%';
            heart.style.animationDuration = (Math.random() * 14 + 8) + 's';
            heart.style.animationDelay = Math.random() * 8 + 's';
            heart.style.opacity = 0.3 + Math.random() * 0.3;
            bgContainer.appendChild(heart);
        }

        const heartElement = document.getElementById('bigHeart');
        const clickCounter = document.getElementById('clickCounter');
        const burst = document.getElementById('burstHearts');
        let clicks = 0;

        // Флаг: было ли уже показано видео-признание (при первых 10 кликах)
        let videoShown = false;

        // модальное окно (десятки) - первое, с фразой
        const modalOverlay = document.getElementById('decadeModal');
        const modalPhrase = document.getElementById('modalPhrase');
        const closeModal = document.getElementById('closeModal');

        // новое модальное окно с видео-признанием
        const videoConfessionModal = document.getElementById('videoConfessionModal');
        const closeConfessionBtn = document.getElementById('closeConfessionBtn');
        const toggleTranscriptBtn = document.getElementById('toggleTranscriptBtn');
        const transcriptText = document.getElementById('transcriptText');

        // попап из фильма
        const moviePopup = document.getElementById('moviePopup');
        const movieQuote = document.getElementById('movieQuote');
        const movieFrom = document.getElementById('movieFrom');
        const closeMoviePopup = document.getElementById('closeMoviePopup');

        // открытка и печать
        const postcardModal = document.getElementById('postcardModal');
        const postcardBtn = document.getElementById('postcardBtn');
        const closePostcard = document.getElementById('closePostcard');
        const typedTextSpan = document.getElementById('typedText');
        const cursorSpan = document.getElementById('cursor');

        // печать
        const sealModal = document.getElementById('sealModal');
        const sealWax = document.getElementById('sealWax');
        const sealCounter = document.getElementById('sealCounter');
        let sealClicks = 0;
        const maxSealClicks = 7; // количество нажатий для разбивания

        // Длинный, красивый, искренний текст открытки (от парня к девушке)
        const fullPostcardText = "Любимая моя,\n\nсегодня, в День всех влюблённых, я хочу сказать тебе то, что давно ношу в сердце. Ты — не просто человек рядом. Ты — мой свет, моё утро, моя тихая гавань и мой бескрайний океан.\n\nС тобой я понял, что значит быть счастливым по-настоящему. Твоя улыбка согревает меня даже в самый холодный день, твой смех звучит для меня самой прекрасной музыкой. Я люблю тебя не за что-то, а просто потому, что ты есть.\n\nЯ обещаю беречь тебя, заботиться о тебе, быть твоей опорой и поддержкой. Я хочу просыпаться с тобой каждый день, встречать рассветы и провожать закаты. Я хочу делить с тобой все радости и печали, быть рядом, когда трудно, и радоваться каждому мгновению, когда нам хорошо.\n\nТы — моя единственная. Ты — моя судьба. И я клянусь, что сделаю всё, чтобы ты была самой счастливой девушкой на свете.\n\nС Днём святого Валентина, моя любовь. Навсегда твой.";

        // === 25 УНИКАЛЬНЫХ ФРАЗ ОТ ПАРНЯ К ДЕВУШКЕ (без повторов) ===
        const lovePhrases = [
            "Ты у меня в сердечке 💓",
            "Наши сердца бьются в унисон 💞",
            "Я твой, ты моя — и это прекрасно 💑",
            "Взаимность — наш секрет ✨",
            "С тобой я нашёл свою половинку 🧩",
            "Ты думаешь обо мне, а я о тебе 💭",
            "Мы как две половинки одного целого 🍊",
            "Взаимная любовь — это магия 🔮",
            "Наши взгляды встречаются даже в толпе 👀",
            "Ты — мой человек навсегда 💘",
            "Ты — причина моего счастья 😊",
            "С тобой каждый день особенный 🌟",
            "Ты — моё утро и мой вечер ☀️🌙",
            "Я выбираю тебя каждый день 💖",
            "Ты — моя муза и вдохновение 🎨",
            "С тобой я становлюсь лучше 🌱",
            "Ты — мой самый тёплый уголок 🏠",
            "Ты — мой свет в окне 🕯️",
            "Ты — моя единственная и неповторимая 🌹",
            "Ты — моя радость и улыбка 😍",
            "Ты — моё сердце и душа ❤️",
            "Ты — моя вселенная 🌌",
            "Ты — моя сказка, ставшая реальностью ✨",
            "Ты — моё счастье, которое я берегу 🤲",
            "Ты — любовь всей моей жизни 💕"
        ];

        // 10 цитат из фильмов (оставим без изменений)
        const movieQuotes = [
            { quote: "Ты заставляешь меня хотеть быть лучше.", movie: "«Вам письмо»" },
            { quote: "Я просто хочу провести с тобой всю оставшуюся жизнь, начиная как можно скорее.", movie: "«Когда Гарри встретил Салли»" },
            { quote: "Ты — единственный человек, с которым я хочу говорить о погоде.", movie: "«Дневник памяти»" },
            { quote: "Я люблю тебя. Я знаю, это просто три слова, но они значат гораздо больше.", movie: "«Звёздная пыль»" },
            { quote: "Ты — моя единственная.", movie: "«Гордость и предубеждение»" },
            { quote: "Мы с тобой — это навсегда.", movie: "«Сумерки»" },
            { quote: "Моё сердце будет биться ради тебя вечно.", movie: "«Красавица и чудовище»" },
            { quote: "Я выбираю тебя. Снова и снова.", movie: "«Дневник памяти»" },
            { quote: "Ты делаешь меня счастливее, чем я могла себе представить.", movie: "«Дорогой Джон»" },
            { quote: "С тобой я дома.", movie: "«В метре друг от друга»" }
        ];

        // обработка кликов на сердце
        heartElement.addEventListener('mousedown', (e) => e.preventDefault());
        heartElement.addEventListener('click', (e) => {
            e.stopPropagation();
            e.preventDefault();
            clicks++;
            clickCounter.textContent = clicks;

            heartElement.style.transform = 'scale(1.5)';
            setTimeout(() => { heartElement.style.transform = 'scale(1)'; }, 150);

            burst.style.animation = 'none';
            burst.offsetHeight;
            burst.style.animation = 'burstGlow 0.5s';
            setTimeout(() => { burst.style.animation = 'burstGlow 1.2s infinite'; }, 500);

            clickCounter.style.background = '#ffb0c3';
            clickCounter.style.color = '#2f1420';
            setTimeout(() => {
                clickCounter.style.background = 'rgba(60,20,30,0.5)';
                clickCounter.style.color = '#ffd9e2';
            }, 200);

            if (clicks % 10 === 0) {
                const randomIndex = Math.floor(Math.random() * lovePhrases.length);
                modalPhrase.textContent = lovePhrases[randomIndex];
                modalOverlay.classList.add('show');
            }
        });

        closeModal.addEventListener('click', () => {
            modalOverlay.classList.remove('show');
            if (!videoShown && clicks === 10) {
                videoConfessionModal.classList.add('show');
                videoShown = true;
                transcriptText.classList.remove('show');
                toggleTranscriptBtn.textContent = '📜 показать текстовую расшифровку';
            }
        });

        modalOverlay.addEventListener('click', (e) => {
            if (e.target === modalOverlay) {
                modalOverlay.classList.remove('show');
                if (!videoShown && clicks === 10) {
                    videoConfessionModal.classList.add('show');
                    videoShown = true;
                    transcriptText.classList.remove('show');
                    toggleTranscriptBtn.textContent = '📜 показать текстовую расшифровку';
                }
            }
        });

        // кнопка сюрприз
        let popupTimeout = null;
        surpriseBtn.addEventListener('click', () => {
            const randomIdx = Math.floor(Math.random() * movieQuotes.length);
            const selected = movieQuotes[randomIdx];
            movieQuote.textContent = `“${selected.quote}”`;
            movieFrom.textContent = `— ${selected.movie}`;
            moviePopup.classList.add('show');
            if (popupTimeout) clearTimeout(popupTimeout);
            popupTimeout = setTimeout(() => {
                moviePopup.classList.remove('show');
            }, 20000);

            const mainMsg = document.getElementById('mainMessage');
            const originalMsg = mainMsg.innerHTML;
            mainMsg.innerHTML = `💕 ${selected.quote} 💕`;
            mainMsg.style.background = 'rgba(200,100,130,0.6)';
            setTimeout(() => {
                mainMsg.innerHTML = originalMsg;
                mainMsg.style.background = 'rgba(80,30,40,0.25)';
            }, 2800);

            for (let i=0; i<6; i++) {
                const mini = document.createElement('span');
                mini.textContent = '💖';
                mini.style.position = 'fixed';
                mini.style.left = Math.random() * 100 + '%';
                mini.style.top = Math.random() * 100 + '%';
                mini.style.fontSize = '2rem';
                mini.style.pointerEvents = 'none';
                mini.style.zIndex = '9999';
                mini.style.transition = 'all 1s ease-out';
                document.body.appendChild(mini);
                setTimeout(() => {
                    mini.style.transform = 'translateY(-100px) scale(2)';
                    mini.style.opacity = '0';
                }, 20);
                setTimeout(() => mini.remove(), 1100);
            }
        });

        closeMoviePopup.addEventListener('click', () => {
            moviePopup.classList.remove('show');
            if (popupTimeout) { clearTimeout(popupTimeout); popupTimeout = null; }
        });

        // Функция печати текста
        function typeWriter(text, element, callback) {
            let i = 0;
            element.textContent = '';
            function typing() {
                if (i < text.length) {
                    element.textContent += text.charAt(i);
                    i++;
                    setTimeout(typing, 40); // чуть быстрее для длинного текста
                } else {
                    if (callback) callback();
                }
            }
            typing();
        }

        // Обработка печати (разбивание)
        postcardBtn.addEventListener('click', () => {
            sealClicks = 0;
            sealCounter.textContent = `0 / ${maxSealClicks}`;
            sealModal.classList.add('show');
        });

        sealWax.addEventListener('click', () => {
            sealClicks++;
            sealCounter.textContent = `${sealClicks} / ${maxSealClicks}`;

            // анимация удара
            sealWax.style.transform = 'scale(0.8)';
            setTimeout(() => sealWax.style.transform = 'scale(1)', 100);

            // трещинка визуальная (эмодзи)
            if (sealClicks === 3) sealWax.textContent = '💔';
            if (sealClicks === 5) sealWax.textContent = '💥';

            if (sealClicks >= maxSealClicks) {
                // Разбитие! убираем печать, показываем открытку
                sealModal.classList.remove('show');
                postcardModal.classList.add('show');
                typedTextSpan.textContent = '';
                setTimeout(() => {
                    typeWriter(fullPostcardText, typedTextSpan);
                }, 300);
            }
        });

        closePostcard.addEventListener('click', () => {
            postcardModal.classList.remove('show');
        });
        postcardModal.addEventListener('click', (e) => {
            if (e.target === postcardModal) postcardModal.classList.remove('show');
        });

        // обработка видео-модалки
        closeConfessionBtn.addEventListener('click', () => {
            videoConfessionModal.classList.remove('show');
        });
        videoConfessionModal.addEventListener('click', (e) => {
            if (e.target === videoConfessionModal) videoConfessionModal.classList.remove('show');
        });

        toggleTranscriptBtn.addEventListener('click', () => {
            if (transcriptText.classList.contains('show')) {
                transcriptText.classList.remove('show');
                toggleTranscriptBtn.textContent = '📜 показать текстовую расшифровку';
            } else {
                transcriptText.classList.add('show');
                toggleTranscriptBtn.textContent = '📜 скрыть расшифровку';
            }
        });

        setInterval(() => {
            const frame = document.querySelector('.video-frame');
            if (frame) {
                frame.style.filter = 'brightness(1.02)';
                setTimeout(() => { frame.style.filter = 'brightness(1)'; }, 100);
            }
        }, 4000);
    })();
</script>
</body>
</html>
